shared:
    image: node:14

jobs:
    A:
        requires: [~commit]
        steps:
            - echo1: echo in parallel
    B:
        requires: [~commit]
        steps:
            - echo: echo in parallel
    C:
        requires: [A, B]
        steps:
            - echo: echo join after A and B
